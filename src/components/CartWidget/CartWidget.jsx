import carrito from "./assets/carrito.svg";

export const CartWidget = () => {
  return (
    <div>
      <img src={carrito} alt="cart-widget" /> 0{" "}
    </div>
  );
};

export default CartWidget;
